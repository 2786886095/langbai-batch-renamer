<Window x:Class="BatchRename.App.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="回退历史" Width="780" Height="540" MinWidth="650" MinHeight="430"
        WindowStartupLocation="CenterOwner" UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

        <Grid Margin="0,0,0,18">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Width="42" Height="42" CornerRadius="10" Background="#EFF6FF" Margin="0,0,12,0">
                <TextBlock Text="&#xE81C;" FontFamily="Segoe Fluent Icons" FontSize="20" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Grid.Column="1">
                <TextBlock Text="回退历史" FontSize="24" FontWeight="SemiBold"/>
                <TextBlock Text="记录保存在本机；关闭软件或重启电脑后仍可回退。若原名称已被占用，系统会阻止回退。" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="White" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="10">
            <Grid>
            <ListBox x:Name="HistoryList" BorderThickness="0" Background="Transparent" Padding="6" SelectionChanged="HistoryList_SelectionChanged" AutomationProperties.Name="批量重命名历史列表">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value><ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" CornerRadius="7" Padding="10,8">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ItemBorder" Property="Background" Value="#F1F5F9"/></Trigger>
                                    <Trigger Property="IsSelected" Value="True"><Setter TargetName="ItemBorder" Property="Background" Value="#DBEAFE"/></Trigger>
                                    <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="ItemBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}"/><Setter TargetName="ItemBorder" Property="BorderThickness" Value="1"/></Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate></Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Summary}" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            <Border Grid.Column="1" Background="{Binding BadgeBackground}" CornerRadius="12" Padding="10,4" VerticalAlignment="Center" Margin="16,0,0,0">
                                <TextBlock Text="{Binding StateText}" Foreground="{Binding BadgeForeground}"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
                <StackPanel x:Name="EmptyHistory" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Border Width="52" Height="52" CornerRadius="26" Background="#EFF6FF" HorizontalAlignment="Center">
                        <TextBlock Text="&#xE81C;" FontFamily="Segoe Fluent Icons" FontSize="23" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="还没有批量重命名记录" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,12,0,4"/>
                    <TextBlock Text="完成一次操作后，可在这里恢复原名称。" Foreground="{StaticResource MutedBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,16,0,0">
            <TextBlock x:Name="HistoryHint" Text="选择一条未回退的记录。" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource SecondaryButton}" Content="关闭" Margin="0,0,10,0" Click="Close_Click"/>
                <Button x:Name="UndoButton" Style="{StaticResource PrimaryButton}" Content="回退所选操作" IsEnabled="False" Click="Undo_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
